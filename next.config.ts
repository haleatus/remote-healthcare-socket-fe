import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images.pexels.com", "docs.mapbox.com", "images.unsplash.com"],
  },
};

export default nextConfig;
